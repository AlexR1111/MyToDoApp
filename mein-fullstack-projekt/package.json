{
  "name": "mein-fullstack-projekt",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && node server.js\" \"cd frontend && npm start\"",
    "dev": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && npm start\""  
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9"
  }
}
